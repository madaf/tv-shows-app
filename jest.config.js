// Sync object
/** @type {import('@jest/types').Config.InitialOptions} */
const config = {
  preset: '@vue/cli-plugin-unit-jest',
  testMatch: ['**/*.test.js'],
  verbose: true,
}

module.exports = config;